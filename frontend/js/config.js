// Backend location.
//   ""  = same server that serves these pages (local run via backend/run.py, or a Vercel / Render single deploy).
//   Or an absolute URL when the frontend is hosted separately, e.g. "https://pa-audit-api.onrender.com".
window.PA_AUDIT_CONFIG = { apiUrl: "" };
